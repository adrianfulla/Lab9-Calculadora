import CKeys from './CKeys'

export default {
  title: 'Calculator/CKeys',
  component: CKeys,
  tags: ['autodocs'],
  argTypes: {
    setScreen: { action: 'PressButton' },
  },
}

export const Default = {
  args: null,
}
